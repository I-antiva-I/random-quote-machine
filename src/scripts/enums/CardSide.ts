/**
 * Enum, which represents the different sides of "card" element
 * 
 * @enum {number} CardSide
 * @property {number} BACK
 * @property {number} FRONT 
 */

export enum CardSide 
{
  BACK      = 0,
  FRONT     = 1,
}